# M1.1 coordination-ref spike - results

Generated by `bun spikes/coordination-ref/run.ts` on 2026-09-04T22:22:14.445Z. This file is produced by the run, not hand-written - re-running regenerates it (with fresh timings and temp-directory paths).

## Environment

- git: git version 2.55.0
- bun: 1.4.0
- OS: Linux 7.2.2-1-cachyos (linux/x64)

## Overall: ALL SCENARIOS PASSED

## Scenario 1: Create the orphan ref, append N events, read them back

**Result: PASS**

Commands run:

```
git commit-tree <empty-tree-sha> -m 'cankan: init coordination ref'
git update-ref refs/cankan/coordination <commit> 0000000000000000000000000000000000000000
(x5) git hash-object -w --stdin | git read-tree <old> && git update-index --add --cacheinfo ... && git write-tree | git commit-tree <tree> -p <old> -m ... | git update-ref refs/cankan/coordination <new> <old>
git cat-file -p 021ac0e1788dfbb94363666e05f555bb7965f9b6:events/2026-09.jsonl
```

Timings:

- init ref: 1.6 ms
- append 5 events: 37.7 ms

Notes:

- read back all 5 events in append order: ck-seed-001, ck-seed-002, ck-seed-003, ck-seed-004, ck-seed-005

## Scenario 2: Three concurrent processes race to claim the same ticket - CAS vs file lock

**Result: PASS**

Commands run:

```
CAS: git hash-object -w --stdin; git read-tree <old> / update-index --add --cacheinfo / write-tree (private temp index); git commit-tree <tree> -p <old> -m 'claim <ticket> by <actor>'; git update-ref refs/cankan/coordination <new> <old>
file lock: open(lockPath, O_EXCL) to acquire; same off-tree build as above; git update-ref refs/cankan/coordination <new>  (no CAS - the lock is the only serialization); unlink(lockPath) to release
```

Timings:

- CAS: race wall clock, avg over 10 runs: 264.6 ms
- CAS: race wall clock, max over 10 runs: 268.6 ms
- file lock: race wall clock, avg over 10 runs: 369.8 ms
- file lock: race wall clock, max over 10 runs: 707.1 ms
- file lock: longest single lock-wait observed: 377.9 ms
- stale-lock: time for holder to acquire and announce: 13.5 ms
- stale-lock: claimant's wait against the stale lock (1.2s timeout configured): 1233.7 ms

Notes:

- CAS: 10/10 races had a losing worker observe a CAS rejection (attempts>1); max attempts by any single worker: 2
- CAS: example rejection stderr: fatal: update_ref failed for ref 'refs/cankan/coordination': cannot lock ref 'refs/cankan/coordination': is at 2fea4f68956ca81591405279f3614d6aa8fd3ce9 but expected 0ca7d48fd081f2eeb3f0408ff557881cec59d816
- file lock: max lock-acquire wait observed by a losing worker: 377.9 ms (10ms retry polling, 10s timeout)
- stale-lock: lockfile was left behind after SIGKILLing the holder - this spike's lockfile has no PID-liveness or lease-based staleness check
- stale-lock: claimant against the stale lock reported: {"outcome":"lock_timeout","error":"Error: claimViaLock: timed out waiting for lock at /tmp/cankan-test-FOSRXB/main/.git/cankan-coordination.lock"}

## Scenario 3: git rebase a feature branch onto main - refs/cankan/* untouched and readable

**Result: PASS**

Commands run:

```
git checkout -b feature
git add feature.txt && git commit -m 'feature work'
git checkout main
git add main.txt && git commit -m 'main moves on'
git checkout feature && git rebase main
git rev-parse refs/cankan/coordination
```

Timings:

- git rebase main: 2.8 ms

Notes:

- coordination ref sha unchanged across rebase (b6ebe7f4819fcd29ed961e78a441d9750aa004b8)
- claim event still readable after rebase

## Scenario 4: Merge a feature branch into main - refs/cankan/* untouched and readable

**Result: PASS**

Commands run:

```
git checkout -b feature
git add feature.txt && git commit -m 'feature work'
git checkout main
git add main.txt && git commit -m 'main moves on'
git merge feature --no-edit
git rev-parse refs/cankan/coordination
```

Timings:

- git merge feature: 4.3 ms

Notes:

- coordination ref sha unchanged across merge (971a18c8eb8ebf6ec063efb645611682cf7ee831)
- claim event still readable after merge

## Scenario 5: Push and fetch the coordination ref through a bare remote

**Result: PASS**

Commands run:

```
git push origin main   # default refspec, no cankan ref named
git push origin refs/cankan/coordination:refs/cankan/coordination
git clone /tmp/cankan-test-XWdKW3/remote.git <clone-dir>
git fetch origin refs/cankan/coordination:refs/cankan/coordination
git push origin refs/cankan/coordination:refs/cankan/coordination   # advance origin again
git fetch origin   # default fetch, no refspec; ref already exists locally in the clone
(the clone appends its own claim off its stale local ref, independent of the remote's new tip)
git push origin refs/cankan/coordination:refs/cankan/coordination   # from the clone - expected to be rejected as non-fast-forward
```

Timings:

- git push origin main (default): 3.4 ms
- git push origin refs/cankan/coordination:refs/cankan/coordination: 9.7 ms
- git fetch origin refs/cankan/coordination:refs/cankan/coordination: 5.6 ms
- git fetch origin (default, ref already exists locally): 5.2 ms

Notes:

- confirmed: a default `git push origin main` does NOT move refs/cankan/coordination on the remote
- push with the explicit refspec 'refs/cankan/coordination:refs/cankan/coordination' moved the remote ref to bd63c1ef3aeebfc27acea794b20551b1c43e4542
- confirmed: a plain `git clone` does NOT bring refs/cankan/coordination along
- fetch with the explicit refspec 'refs/cankan/coordination:refs/cankan/coordination' correctly created refs/cankan/coordination in the clone at bd63c1ef3aeebfc27acea794b20551b1c43e4542
- confirmed: a default 'git fetch origin' does NOT advance an already-existing local refs/cankan/coordination, even though the remote had moved on to 0aeef6d00599c6043af9b3b20510f3db2756d0c6 - the explicit refspec is required on every fetch, not just the first one
- confirmed: git rejects a non-fast-forward push of the coordination ref by default, no force needed to protect it; exact stderr:  ! [rejected]        refs/cankan/coordination -> refs/cankan/coordination (fetch first)
- required refspec for both push and fetch: refs/cankan/coordination:refs/cankan/coordination, every time (default push/fetch never touch it, even once it exists locally). Cross-machine writes are caught by git's own non-fast-forward push protection, matching CONCEPT.md's "optimistic push, retry on rejection" - M2.6's push path must catch that rejection and re-fetch/re-apply rather than force-pushing.

## Scenario 6: Claim from a secondary worktree - primary worktree sees it immediately

**Result: PASS**

Commands run:

```
(claim ck-worktree-test from the secondary worktree directory)
git rev-parse refs/cankan/coordination   # read from the primary worktree, no fetch/pull
```

Timings:

- claim from secondary worktree: 9.5 ms
- read from primary worktree (no fetch): 4.1 ms

Notes:

- primary worktree saw the claim made from the secondary worktree immediately, with no fetch/pull - confirms the shared-.git premise

